import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  root: {
    color: 'white',
  },
  paper: {
    boxShadow: '0 5px 20px 0 rgb(0 0 0 / 38%)',

    borderRadius: '0px 16px 16px',
  },
}));

export default useStyles;
